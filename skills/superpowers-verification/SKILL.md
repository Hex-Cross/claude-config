---
name: superpowers-verification
description: "DEPRECATED: Use /pre-flight instead — it includes all verification checks plus security, accessibility, and self-review. This skill redirects to /pre-flight."
user-invocable: true
---

**This skill has been merged into `/pre-flight`.**

`/pre-flight` is a strict superset — it runs all 4 checks from this skill (lint, type-check, build, code quality scan) plus 6 additional gates (security, deps, red-team, accessibility, test suite, self-review).

**Action:** Run `/pre-flight` instead.
